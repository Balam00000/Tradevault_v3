package com.tradevault.entity.enums;

public enum CollectionInstructionStatus {
    PENDING,
    PROCESSING,
    COLLECTED,
    RETURNED,
    SENT,
    ACKNOWLEDGED_BY_BANK,
    SETTLED,
    DISPUTED,
    Sent,
    AcknowledgedByBank,
    Settled,
    Disputed
}
